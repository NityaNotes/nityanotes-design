/** Reader-facing copy for the site-wide application header. */
export const appHeaderContent = {
  brand: "nityanotes",
  theme: {
    lightLabel: "Switch to light theme",
    darkLabel: "Switch to dark theme",
  },
} as const;
